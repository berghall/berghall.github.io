language: node_js
node_js:
  - stable

before_deploy:
  - cd $TRAVIS_BUILD_DIR

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep_history: true
  local_dir: "public"
  on:
    branch: master
